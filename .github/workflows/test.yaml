name: Do something
on:
  push:
    branches: [ main ]

jobs:
  docker-test:
    name: Run docker 
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Go Build
      run: |
        echo "*** ${{ github.ref }} ***"
        echo "*** ${{ github.ref_name }} ***"
        echo "*** ${{ github.base_ref || 'dummy' }} ***"
         
