name: Do something

on:
  workflow_dispatch:
    inputs:
      switch:
        description: 'Yes or no'
        required: false
        default: yes
        type: choice
        options:
        - yes
        - no

jobs:
  one:
    runs-on: ubuntu
    name: Job one
    outputs:
      var: ${{ steps.set.outputs.var }}
    steps:
    - name: Set variable
      id: set
      run: |
        if ${{ inputs.switch == 'yes' }}; then
          echo "var=abc" >> $GITHUB_OUTPUT 
        fi

  two:
    runs-on: ubuntu
    name: Job two
    needs: one
    # if: needs.one.outputs.var != ""
    steps:
    - name: Get
      run: |
        echo "value: ${{ needs.one.outputs.var }}"
